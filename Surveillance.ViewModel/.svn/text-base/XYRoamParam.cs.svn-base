using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Surveillance.ViewModel
{
    /// <summary>
    /// 新昱/智敏等解码器漫游窗参数
    /// </summary>
    public class XYRoamParam
    {
        /// <summary>
        /// 漫游覆盖的屏号
        /// </summary>
        public int Monitor;

        /// <summary>
        /// 与覆盖屏幕交接处左上角相对与覆盖屏左上角的的宽
        /// </summary>
        public int OutputStartX;

        /// <summary>
        /// 与覆盖屏幕交接处左上角相对与覆盖屏左上角的的高
        /// </summary>
        public int OutputStartY;

        /// <summary>
        /// 与覆盖屏幕交接处左上角相对与漫游屏左上角的的宽
        /// </summary>
        public int DisPlayX;

        /// <summary>
        /// 与覆盖屏幕交接处左上角相对与漫游屏左上角的的高
        /// </summary>
        public int DisPlayY;

        /// <summary>
        /// 漫游屏在覆盖屏的宽
        /// </summary>
        public int Width;

        /// <summary>
        /// 漫游屏在覆盖屏的高
        /// </summary>
        public int Height;
    }
}
